# E2E report — Loqui v0.2.0 release preparation

- **Branch:** `release/v0.2.0`.
- **Timestamp:** `2026-08-13T14:16:04Z`.

VERDICT: PASS

## UC-REL-020-CLI-01 — Resolve consistent release metadata

- **Classification:** PASS.
- **Interface:** CLI.
- **Command observations:**
  - First `./scripts/release-version.sh` → exit 0, stdout `0.2.0`.
  - First `./scripts/release-version.sh --dmg-name` → exit 0, stdout
    `Loqui-0.2.0-macos-arm64.dmg`.
  - `scripts/patch-plists.sh --check` → exit 0, stdout `patch-plists: check ok`.
  - The second version and artifact-name invocations returned the same exact values.
- **Persistence re-check:** The independent second invocation observed the canonical source and both
  generated macOS plist files still in agreement at `0.2.0`.
- **Sanitization:** No credentials, identity hashes, tokens, or user data were emitted.

## UC-REL-020-CLI-02 — Confirm the release identifier remains available

- **Classification:** PASS.
- **Interface:** CLI.
- **Command observations:**
  - First `git ls-remote origin refs/tags/v0.2.0` → exit 0, empty stdout.
  - First `gh release view v0.2.0` → exit 1, expected `release not found`.
  - Second tag query → exit 0, empty stdout.
  - Second Release query → exit 1, expected `release not found`.
- **Persistence re-check:** A second observation through both supported read interfaces confirmed no
  concurrent tag or Release appeared during release preparation.
- **Cleanup:** Read-only journey; no tag, Release, workflow run, or remote branch was created.
- **Sanitization:** Output contained no credentials, tokens, or private repository data.

## Interface coverage

- **API:** N/A — direct API publication is not a supported maintainer journey; publication is
  intentionally confined to the protected Action.
- **UI:** N/A — the bump changes release metadata, not application UI. Azure model selection and
  native window movement are covered by their existing committed E2E reports.
