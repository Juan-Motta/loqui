# E2E report — Loqui v0.3.0 release preparation

- **Branch:** `codex/release-v0.3.0`.
- **Timestamp:** `2026-08-13T16:32:00Z`.

VERDICT: PASS

## UC-REL-030-CLI-01 — Resolve consistent release metadata

- **Classification:** PASS.
- **Interface:** CLI.
- **Observed:** `scripts/patch-plists.sh --check` first failed after only the canonical source
  changed, naming all four stale plist values. After generation it exited 0 with `check ok`.
- **Outcome:** Two independent version/name observations returned `0.3.0`,
  `Loqui-0.3.0-macos-arm64.dmg`, and `Loqui-0.3.0-macos-arm64.zip` exactly.
- **Persistence re-check:** The second invocation read the same values from the persisted source and generated plists.
- **Sanitization:** No credentials, signing identities, tokens, or user data were emitted.

## UC-REL-030-CLI-02 — Confirm the release identifier remains available

- **Classification:** PASS.
- **Interface:** CLI.
- **Observed:** `git ls-remote origin refs/tags/v0.3.0` exited 0 with empty output;
  `gh release view v0.3.0` returned the expected `release not found`.
- **Outcome:** No existing public identifier conflicts with the proposed release.
- **Persistence re-check:** A second independent tag query again returned empty output and the
  second Release query again returned `release not found`; the same reads remain mandatory immediately
  before the single workflow dispatch.
- **Cleanup:** Read-only journey; no remote object or workflow run was created.
- **Sanitization:** Output contained no credentials or private data.

## Interface coverage

- **API:** N/A — direct API publication is not the supported maintainer journey.
- **UI:** N/A — release metadata has no application UI path.
