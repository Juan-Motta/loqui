# E2E report — Loqui v0.1.1 release preparation

- **Branch:** `release/v0.1.1`
- **Timestamp:** `2026-08-12T18:39:55Z`

VERDICT: PASS

## UC-REL-011-CLI-01 — Resolve consistent release metadata

- **Classification:** PASS
- **Interface:** CLI
- **Command observations:**
  - `./scripts/release-version.sh` → exit 0, stdout `0.1.1`.
  - `./scripts/release-version.sh --dmg-name` → exit 0, stdout
    `Loqui-0.1.1-macos-arm64.dmg`.
  - `./scripts/patch-plists.sh --check` → exit 0, stdout `patch-plists: check ok`.
- **Persistence re-check:** The independent check invocation observed the generated source and
  both macOS plist files in agreement at `0.1.1`.
- **Sanitization:** No credentials, identity hashes, tokens, or user data were emitted.

## UC-REL-011-CLI-02 — Confirm the release identifier remains available

- **Classification:** PASS
- **Interface:** CLI
- **Command observations:**
  - First `git ls-remote --tags origin refs/tags/v0.1.1` → exit 0, empty stdout.
  - First `gh release view v0.1.1` → exit 1, expected `release not found`.
  - Second tag query → exit 0, empty stdout.
  - Second Release query → exit 1, expected `release not found`.
- **Persistence re-check:** A second observation through both supported read interfaces confirmed
  no concurrent tag or Release appeared before branch shipment.
- **Cleanup:** Read-only journey; no remote or local resource was created.
- **Sanitization:** Output contained no credentials, tokens, or private repository data.

## Interface coverage

- **API:** N/A — direct API publication is not a supported maintainer journey; publication is
  intentionally confined to the protected Action.
- **UI:** N/A — the bump changes release metadata, not application UI. The underlying installer UI
  has its own passing native Finder report at
  `docs/e2e/reports/2026-08-11-intuitive-dmg-installer.md`.
